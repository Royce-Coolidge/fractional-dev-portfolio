name = "fractional-dev-portfolio"
compatibility_date = "2024-01-01"
pages_build_output_dir = "build/client"

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }
